# SarVita Arc

A SarVita-branded SillyTavern frontend for power users, with the original chat workspace, authentication flow, API surface, and responsive SarVita Arc presentation layer.

## Screenshots

<p align="center">
  <img src="https://i.imgur.com/2mHH5Wm.jpeg" alt="SarVita Arc screenshot 1" style="display:block;width:800px;max-width:100%;height:auto;margin:12px auto;" />
  <img src="https://i.imgur.com/RLf3nmD.jpeg" alt="SarVita Arc screenshot 2" style="display:block;width:800px;max-width:100%;height:auto;margin:12px auto;" />
  <img src="https://i.imgur.com/KkAbhAU.jpeg" alt="SarVita Arc screenshot 3" style="display:block;width:800px;max-width:100%;height:auto;margin:12px auto;" />
</p>

## Highlights

- SarVita Arc branding layered onto the SillyTavern interface.
- Responsive desktop and mobile layout support.
- Original frontend functionality preserved, including chat, plugins, API routes, and authentication.
- Browser-ready `public/lib.js` bundle generated by the project build.
- Homepage feature video embedded responsively without changing the video’s native aspect ratio.

### Homepage video

The main interface includes a muted, inline, looping feature video with native controls. The video is loaded from:

<https://files.catbox.moe/lbr5xd.mp4>

It uses `preload="metadata"`, `playsinline`, and responsive sizing so it remains usable on phones and desktop screens while avoiding layout distortion.

## التشغيل المحلي

المتطلبات: Node.js 20 أو أحدث وnpm.

```bash
npm ci
npm run build
npm start
```

## Production وRailway

أمر البناء هو `npm run build`، وأمر التشغيل هو `npm start` أو `npm run start:production`. يقرأ الخادم `PORT` ويستمع على `0.0.0.0` عند تشغيله مع `--listen`، مع fallback محلي على المنفذ 8000. ملف `railway.json` يضبط Nixpacks، البناء، أمر التشغيل، وفحص الصحة `/api/ping`.

للنشر على Railway:

1. أنشئ مشروعاً جديداً واربط مستودع SarVita.
2. استخدم Node.js 20 أو أحدث.
3. اترك Railway يحقن `PORT` تلقائياً.
4. اضبط متغيرات البيئة المطلوبة من دون وضع أسرار في المستودع.
5. تحقق من `/api/ping` ثم افتح `/`.

## Vercel

يبقى `vercel.json` محفوظاً كما هو، ويوجه أصول `public/` وواجهات `api/` إلى وظائف Vercel. استخدم إعدادات Vercel الحالية، وتأكد من تشغيل `npm run build` ضمن إعدادات البناء قبل النشر.

## المسارات والتحقق

- `/` — واجهة SarVita Arc الرئيسية
- `/login` — صفحة تسجيل الدخول
- `/api/ping` — فحص صحة الخادم
- `/lib.js` — الحزمة المولدة للمتصفح
- `/script.js` — منطق الواجهة
- `/style.css` — التنسيق الرئيسي
- الفيديو الرئيسي: <https://files.catbox.moe/lbr5xd.mp4>

بعد التشغيل، تحقق من أن `/api/ping` يعيد استجابة ناجحة، وأن `/lib.js` و`/script.js` و`/style.css` تُحمّل، ثم افحص ظهور الفيديو والواجهة الرئيسية من الهاتف والحاسوب. متغيرات البيئة الخاصة بالمصادقة وواجهات API تبقى مطلوبة حسب إعداد المشروع الحالي؛ أضف أسماء المتغيرات في منصة الاستضافة من دون كشف قيمها في README أو Git.

## Resources

- Original project: <https://github.com/SillyTavern/SillyTavern>
- Documentation: <https://docs.sillytavern.app/>
- SarVita repository: <https://github.com/yous199444-byte/SarVita>

## Languages

- العربية
- English
- Español
- Русский
- Português

## License

AGPL-3.0
